import { StyleSheet } from "react-native";
import { theme } from "@/theme";

export const styles = StyleSheet.create({
  list: {
    paddingBottom: 4,
    maxHeight: 34,
  },
  content:{
    gap: 4,
    paddingHorizontal: 8,
  },
})